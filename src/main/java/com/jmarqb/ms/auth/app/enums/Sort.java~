package com.jmarqb.ms.auth.app.enums;

public enum Sort {
    ASC,
    DESC;

//    public static boolean isValid(String value) {
//        return value.equals(ASC.name()) || value.equals(DESC.name());
//    }
}
